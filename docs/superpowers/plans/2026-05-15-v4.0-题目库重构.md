# v4.0 题目库重构 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 上传材料后自动异步出题，刷题零等待；错题重做和间隔复习概念分离，各有独立闭环。

**Architecture:** 分三层推进 — 数据模型 (Tasks 1-2) → 后端 API (Tasks 3-6) → 前端 UI (Tasks 7-12)。每层做完才进下一层，保证接口合约稳定。ForgePanel 从 1019 行拆为 ForgePanel (答题) + ReviewPanel (错题+复习) + MasteredList (已攻克) + StableCardList (稳定卡片)，各约 300-500 行。

**Tech Stack:** Python FastAPI + SQLAlchemy + SQLite + ChromaDB, Next.js 16 + TypeScript + Tailwind v4

---

## 文件变更地图

| 操作 | 文件 | 职责 |
|------|------|------|
| 修改 | `backend/models/database.py:21-50` | Question + ReviewCard 新增字段 |
| 修改 | `backend/routers/materials.py:85-95` | 新增 generate + status 端点 |
| 修改 | `backend/routers/quiz.py:31-47` | submit 更新 challenge_streak |
| 修改 | `backend/routers/review.py:49-80` | record 毕业判定 + wrong-questions |
| 修改 | `backend/routers/stats.py:104-118` | recent_accuracy 始终返回 |
| 修改 | `frontend/src/lib/api.ts` | 新类型 + 新端点 |
| 创建 | `frontend/src/components/ReviewPanel.tsx` | 错题重做 + 间隔复习 |
| 创建 | `frontend/src/components/MasteredList.tsx` | 已攻克题目列表 |
| 创建 | `frontend/src/components/StableCardList.tsx` | 稳定卡片列表 + 手动复习 |
| 修改 | `frontend/src/components/ForgePanel.tsx` | 空闲态重构 + 答题逻辑简化 |
| 修改 | `frontend/src/components/KnowledgePanel.tsx` | 材料卡片出题状态 |
| 修改 | `frontend/src/components/UploadModal.tsx` | 上传后触发 generate |
| 修改 | `frontend/src/app/page.tsx` | 全局复习拆两个入口 |
| 创建 | `tests/v4.0-acceptance.py` | 全量验收脚本 |

---

## Task 1: DB Schema — Question 表新增字段 (P0)

**Files:**
- Modify: `backend/models/database.py:21-31`

- [ ] **Step 1: 加字段**

```python
class Question(Base):
    __tablename__ = "questions"
    id = Column(Integer, primary_key=True, autoincrement=True)
    material_id = Column(Integer, ForeignKey("materials.id"))
    chunk_id = Column(String(200))
    question = Column(Text)
    options = Column(JSON)
    answer = Column(String(20))
    explanation = Column(Text)
    tags = Column(JSON)
    challenge_streak = Column(Integer, default=0)  # NEW
    mastered = Column(Boolean, default=False)        # NEW
    created_at = Column(DateTime, default=datetime.utcnow)
```

- [ ] **Step 2: 删 DB + 重启验证**

```bash
rm -f /Users/chenbaijian/my-claude/recall-forge/backend/data/quiz.db
cd /Users/chenbaijian/my-claude/recall-forge/backend && python3 -m uvicorn main:app --reload --port 8000
curl -s http://localhost:8000/api/health
```

Expected: `{"status":"ok"}`, DB 自动创建新表。

---

## Task 2: DB Schema — ReviewCard 表新增 + 修复字段 (P0)

**Files:**
- Modify: `backend/models/database.py:41-50`

- [ ] **Step 1: 加 total_reviews + is_stable（total_reviews v3.3 已加，确认存在）**

```python
class ReviewCard(Base):
    __tablename__ = "review_cards"
    id = Column(Integer, primary_key=True, autoincrement=True)
    question_id = Column(Integer, ForeignKey("questions.id"))
    ease_factor = Column(Float, default=2.5)
    interval_days = Column(Integer, default=1)
    repetitions = Column(Integer, default=0)
    total_reviews = Column(Integer, default=0)   # v3.3 已加，确认存在
    is_stable = Column(Boolean, default=False)   # NEW
    next_review = Column(DateTime, default=datetime.utcnow)
    last_review = Column(DateTime, nullable=True)
```

- [ ] **Step 2: 重启验证**

```bash
cd /Users/chenbaijian/my-claude/recall-forge/backend && python3 -c "from models.database import ReviewCard; print('total_reviews' in ReviewCard.__table__.columns); print('is_stable' in ReviewCard.__table__.columns)"
```

Expected: `True` `True`

---

## Task 3: 异步出题端点 (P0)

**Files:**
- Modify: `backend/routers/materials.py:85-95`
- Modify: `backend/services/quiz_gen.py:73-91`

- [ ] **Step 1: 新增 generate 端点（同步执行，当前用同步就够了）**

在 `materials.py` 末尾新增：

```python
from services.quiz_gen import generate_quiz_sync
import threading

@router.post("/{material_id}/generate")
def generate_questions(material_id: int, exclude_covered: bool = False, db: Session = Depends(get_db)):
    material = db.query(Material).filter(Material.id == material_id).first()
    if not material:
        raise HTTPException(status_code=404, detail="材料不存在")
    
    total_chunks = count_chunks(material_id)
    max_questions = min(total_chunks * 3, 30)
    if max_questions < 1:
        max_questions = 1
    
    # Get chunk IDs to exclude
    exclude_ids = []
    if exclude_covered:
        used = db.query(Question.chunk_id).filter(Question.material_id == material_id, Question.chunk_id != "").all()
        exclude_ids = [r[0] for r in used]
    
    # 后台线程执行（Fire and forget）
    def run_generation():
        gen_db = next(get_db())
        try:
            result = generate_quiz_sync(material_id, max_questions, exclude_ids, "extract", "medium", "single", "")
            if "questions" in result:
                for q in result["questions"]:
                    chunk_id = result.get("chunk_ids", [""])[0] if result.get("chunk_ids") else ""
                    question = Question(
                        material_id=material_id,
                        chunk_id=chunk_id,
                        question=q["question"],
                        options=q["options"],
                        answer=q.get("answer", "") or ",".join(q.get("answers", [])),
                        explanation=q.get("explanation", ""),
                        tags=q.get("tags", []),
                    )
                    gen_db.add(question)
                gen_db.commit()
        finally:
            gen_db.close()
    
    thread = threading.Thread(target=run_generation)
    thread.start()
    
    return {"status": "generating", "max_questions": max_questions}
```

- [ ] **Step 2: 新增 status 端点**

```python
@router.get("/{material_id}/status")
def get_generation_status(material_id: int, db: Session = Depends(get_db)):
    material = db.query(Material).filter(Material.id == material_id).first()
    if not material:
        raise HTTPException(status_code=404, detail="材料不存在")
    
    total_chunks = count_chunks(material_id)
    question_count = db.query(Question).filter(Question.material_id == material_id).count()
    max_expected = min(total_chunks * 3, 30)
    
    if question_count > 0:
        return {"status": "ready", "question_count": question_count}
    # 简化判断：有题目=ready，没有=可能需要生成
    return {"status": "idle", "question_count": 0}
```

- [ ] **Step 3: 新增 generate_quiz_sync 函数**

在 `quiz_gen.py` 中新增同步版本（去掉 SSE 包装）：

```python
def generate_quiz_sync(material_id, count, exclude_ids, mode, difficulty, question_type, tag=""):
    """同步出题，用于后台任务"""
    import httpx
    chunks = get_random_chunks(material_id, n=min(count * 3, 20), exclude_ids=exclude_ids or [])
    chunk_ids = [c["metadata"].get("chunk_oid", "") for c in chunks]
    chunks_text = "\n\n---\n\n".join([c["content"][:500] for c in chunks])
    
    if not chunks_text.strip():
        return {"questions": [], "chunk_ids": []}
    
    prompt = _build_prompt(count, mode, difficulty, question_type, chunks_text, tag)
    
    headers = {
        "x-api-key": ANTHROPIC_AUTH_TOKEN,
        "anthropic-version": "2023-06-01",
        "content-type": "application/json",
    }
    body = {
        "model": ANTHROPIC_MODEL,
        "max_tokens": 2048,
        "messages": [{"role": "user", "content": prompt}],
    }
    
    resp = httpx.post(f"{ANTHROPIC_BASE_URL}/messages", headers=headers, json=body, timeout=90)
    if resp.status_code != 200:
        return {"questions": [], "chunk_ids": [], "error": f"HTTP {resp.status_code}"}
    
    data = resp.json()
    full_text = data["content"][0]["text"] if data.get("content") else ""
    parsed = _parse_batch_quiz_json(full_text)
    parsed["chunk_ids"] = chunk_ids
    return parsed
```

- [ ] **Step 4: 验证**

```bash
# 创建材料并触发自动出题
curl -s -X POST http://localhost:8000/api/materials -F "title=测试" -F "content=AI产品经理需要掌握大模型能力边界、RAG架构、Prompt工程化、评估体系、数据飞轮等核心能力。这些能力共同构成AI PM的核心竞争力。"
# 拿到 id 后触发 generate
curl -s -X POST http://localhost:8000/api/materials/1/generate
# 等待 30s 后检查
curl -s http://localhost:8000/api/materials/1/status
```

Expected: 30s 后 status=ready，question_count > 0。

---

## Task 4: submit 端点 + challenge_streak (P0)

**Files:**
- Modify: `backend/routers/quiz.py:31-47`

- [ ] **Step 1: 更新 submit_answer**

```python
@router.post("/submit", response_model=QuizSubmitResponse)
def submit_answer(req: QuizSubmitRequest, db: Session = Depends(get_db)):
    question = db.query(Question).filter(Question.id == req.question_id).first()
    if not question:
        raise HTTPException(status_code=404, detail="题目不存在")

    is_correct = req.user_answer.upper() == question.answer.upper()

    attempt = Attempt(
        question_id=req.question_id,
        user_answer=req.user_answer,
        is_correct=1 if is_correct else 0,
    )
    db.add(attempt)

    # 更新 challenge_streak
    if is_correct:
        question.challenge_streak = (question.challenge_streak or 0) + 1
        if question.challenge_streak >= 3:
            question.mastered = True
    else:
        question.challenge_streak = 0
        question.mastered = False

    db.commit()
    db.refresh(question)

    return QuizSubmitResponse(
        is_correct=is_correct,
        correct_answer=question.answer,
        explanation=question.explanation or "",
    )
```

QuizSubmitResponse 新增字段：
```python
class QuizSubmitResponse(BaseModel):
    is_correct: bool
    correct_answer: str
    explanation: str
    challenge_streak: int = 0
    mastered: bool = False
```

- [ ] **Step 2: 验证**

```bash
# 答对一题，检查 streak
curl -s -X POST http://localhost:8000/api/quiz/submit -H "Content-Type: application/json" -d '{"question_id":1,"user_answer":"B"}'
# 预期 challenge_streak=1 或 0
```

---

## Task 5: 间隔复习毕业 + 错题列表 (P0)

**Files:**
- Modify: `backend/routers/review.py:49-80`
- Modify: `backend/routers/review.py:11-45`

- [ ] **Step 1: record 端点加毕业判定**

```python
result = calculate_sm2(req.score, card.ease_factor, card.interval_days, card.repetitions)
card.ease_factor = result["ease_factor"]
card.interval_days = result["interval_days"]
card.repetitions = result["repetitions"]
card.total_reviews = (card.total_reviews or 0) + 1
# 毕业：间隔 > 21 天且连续成功 >= 3 次
if card.interval_days > 21 and card.repetitions >= 3:
    card.is_stable = True
card.next_review = result["next_review"]
card.last_review = datetime.utcnow()
db.commit()

return {
    "ok": True,
    "next_review": result["next_review"].isoformat(),
    "interval_days": result["interval_days"],
    "is_stable": card.is_stable,  # NEW
}
```

- [ ] **Step 1b: 返回新增 is_stable 字段**（见上方 return 中的 `is_stable`）

- [ ] **Step 2: get_due_reviews 过滤 is_stable=False + total_reviews >= 1**

```python
if show_all:
    query = db.query(ReviewCard).filter(ReviewCard.is_stable == False, ReviewCard.total_reviews >= 1)
else:
    query = db.query(ReviewCard).filter(ReviewCard.next_review <= now, ReviewCard.is_stable == False, ReviewCard.total_reviews >= 1)
```

（total_reviews >= 1 防止刚创建从未复习的卡片直接出现在到期列表）

- [ ] **Step 3: 新增 wrong-questions 端点**

```python
@router.get("/wrong-questions")
def get_wrong_questions(material_id: int = None, db: Session = Depends(get_db)):
    query = db.query(Question).filter(Question.mastered == False)
    if material_id:
        query = query.filter(Question.material_id == material_id)
    # 只返回有错误 Attempt 的题目
    subquery = db.query(Attempt.question_id).filter(Attempt.is_correct == 0).subquery()
    query = query.filter(Question.id.in_(subquery))
    questions = query.order_by(Question.challenge_streak.asc()).limit(50).all()
    return [{"id": q.id, "question": q.question, "options": q.options, "answer": q.answer, "explanation": q.explanation, "tags": q.tags, "challenge_streak": q.challenge_streak, "mastered": q.mastered} for q in questions]
```

- [ ] **Step 4: 验证**

```bash
curl -s http://localhost:8000/api/review/wrong-questions?material_id=1
```

---

## Task 6: 统计端点更新 (P1)

**Files:**
- Modify: `backend/routers/stats.py:104-140`

- [ ] **Step 1: recent_accuracy 始终返回**

修改 return dict，recent_accuracy 不再仅当不等于 accuracy 时返回。

- [ ] **Step 2: 新增 wrong_questions + mastered_questions + stable_cards 计数**

在 stats 返回中加：
```python
"wrong_questions": db.query(Question).filter(Question.material_id == material_id, Question.mastered == False).count(),
"mastered_questions": db.query(Question).filter(Question.material_id == material_id, Question.mastered == True).count(),
"stable_cards": db.query(ReviewCard).filter(ReviewCard.question_id.in_(question_ids), ReviewCard.is_stable == True).count(),
```

- [ ] **Step 3: 验证**

```bash
curl -s http://localhost:8000/api/stats/material/1 | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('recent_accuracy','MISSING'), d.get('wrong_questions','MISSING'))"
```

---

## Task 7: 前端 API 层更新 (P0)

**Files:**
- Modify: `frontend/src/lib/api.ts`

- [ ] **Step 1: 更新类型定义**

```tsx
export interface MaterialStats {
  // ...existing...
  recent_accuracy: number;         // 始终存在
  wrong_questions: number;         // NEW
  mastered_questions: number;      // NEW
}

export interface QuestionItem {
  id: number;
  question: string;
  options: string[];
  answer: string;
  explanation: string;
  tags: string[];
  challenge_streak: number;       // NEW
  mastered: boolean;              // NEW
}

export interface GenerateStatus {
  status: "idle" | "generating" | "ready" | "error";
  question_count: number;
  max_questions?: number;
}
```

- [ ] **Step 2: 新增 API 函数**

```tsx
export async function generateQuestions(materialId: number, excludeCovered = false): Promise<GenerateStatus> {
  const res = await fetch(`${API_BASE}/materials/${materialId}/generate`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ exclude_covered: excludeCovered }),
  });
  return res.json();
}

export async function getGenerateStatus(materialId: number): Promise<GenerateStatus> {
  const res = await fetch(`${API_BASE}/materials/${materialId}/status`);
  return res.json();
}

export async function getWrongQuestions(materialId?: number): Promise<QuestionItem[]> {
  const params = materialId ? `?material_id=${materialId}` : "";
  const res = await fetch(`${API_BASE}/review/wrong-questions${params}`);
  return res.json();
}

export async function getStableCards(materialId?: number): Promise<ReviewItem[]> {
  const params = materialId ? `?material_id=${materialId}` : "";
  const res = await fetch(`${API_BASE}/review/stable-cards${params}`);
  return res.json();
}
```

- [ ] **Step 3: listQuestions 加参数**

```tsx
export async function listQuestions(
  materialId: number,
  options?: { count?: number; tags?: string; excludeMastered?: boolean }
): Promise<QuestionItem[]> {
  const params = new URLSearchParams();
  if (options?.count) params.set("count", String(options.count));
  if (options?.tags) params.set("tags", options.tags);
  if (options?.excludeMastered) params.set("exclude_mastered", "true");
  const qs = params.toString();
  const res = await fetch(`${API_BASE}/materials/${materialId}/questions${qs ? "?" + qs : ""}`);
  return res.json();
}
```

---

## Task 8: 创建 ReviewPanel 组件 (P0)

**Files:**
- Create: `frontend/src/components/ReviewPanel.tsx`

将以下逻辑从 ForgePanel 抽出到独立组件：

- [ ] **Step 1: 创建 ReviewPanel**

组件 props: `{ materialId: number; onClose: () => void }`

包含两种子模式：
- `wrong-mode`（错题重做）：从 `/api/review/wrong-questions` 加载，不给答案，选后显示对错，含 streak 追踪
- `sm2-mode`（间隔复习）：从 `/api/review/due` 加载，现有 SM-2 流程

```tsx
"use client";
import { useState, useRef } from "react";
import { getWrongQuestions, getDueReviews, recordReview, submitAnswer, type QuestionItem, type ReviewItem } from "@/lib/api";

type ReviewMode = "wrong" | "sm2";

export function ReviewPanel({ materialId, onClose }: { materialId: number; onClose: () => void }) {
  const [mode, setMode] = useState<ReviewMode>("wrong");
  const [items, setItems] = useState<(QuestionItem | ReviewItem)[]>([]);
  const [idx, setIdx] = useState(0);
  const [showAnswer, setShowAnswer] = useState(false);
  const [selectedAnswer, setSelectedAnswer] = useState<string | null>(null);
  const [loading, setLoading] = useState(true);
  const challengeRef = useRef({ total: 0, correct: 0, mastered: 0 });

  // 加载数据
  useEffect(() => {
    setLoading(true);
    const fetcher = mode === "wrong" ? getWrongQuestions(materialId) : getDueReviews(materialId, true);
    fetcher.then(data => { setItems(data); setLoading(false); }).catch(() => setLoading(false));
  }, [mode, materialId]);

  // ... 错题重做模式渲染（参考现有 re-challenge 代码，但不显示正确答案）
  // ... SM-2 复习模式渲染（参考现有 SM-2 代码）
  // ... 完成统计
}
```

- [ ] **Step 2: 验证**

从 ForgePanel 的"错题重做"和"间隔复习"按钮跳转到此组件，确认独立工作。

---

## Task 9: 重构 ForgePanel — 空闲态改造 (P0)

**Files:**
- Modify: `frontend/src/components/ForgePanel.tsx` — 主要改空闲态 JSX

- [ ] **Step 1: 按钮文案改为"开始答题"**

```tsx
<button onClick={startQuiz}
  className="w-full py-2.5 bg-brand text-black rounded-lg text-sm font-medium hover:opacity-90 transition-opacity">
  {stats.total_questions > 0 ? "开始答题" : "暂无题目"}
</button>
```

- [ ] **Step 2: 添加"补充题目"按钮**

```tsx
{stats.total_questions > 0 && (
  <button onClick={supplementQuestions} disabled={stats.covered_chunks >= stats.total_chunks}
    className="w-full py-2 rounded-lg text-sm border border-border-soft text-text-secondary hover:bg-surface-raised transition-colors disabled:opacity-30">
    {stats.covered_chunks >= stats.total_chunks ? "已无新题可出" : "补充题目"}
  </button>
)}
```

- [ ] **Step 3: 错题重做 + 间隔复习改为独立入口**

```tsx
{stats.wrong_questions > 0 && (
  <button onClick={() => setPhase("wrongReview")} className="...">
    🔴 错题重做 · {stats.wrong_questions}题待攻克
  </button>
)}
{stats.due_reviews > 0 && (
  <button onClick={() => setPhase("sm2Review")} className="...">
    🔵 间隔复习 · {stats.due_reviews}题到期
  </button>
)}
```

- [ ] **Step 4: 正确率双维度始终显示**

```tsx
<div className="text-right shrink-0 ml-2">
  <div className="text-text-muted text-sm">总 {stats.accuracy}%</div>
  <div className={`text-[10px] ${stats.recent_accuracy > stats.accuracy ? "text-green-400" : stats.recent_accuracy < stats.accuracy ? "text-text-dim" : "text-text-muted"}`}>
    近期 {stats.recent_accuracy}%
    {stats.recent_accuracy > stats.accuracy + 5 ? " ↑" : stats.recent_accuracy < stats.accuracy - 5 ? " ↓" : ""}
  </div>
</div>
```

- [ ] **Step 5: startQuiz 改用 listQuestions**

```tsx
const startQuiz = useCallback(async () => {
  setPhase("answering");
  const count = config.count;
  const tags = config.tag ? [config.tag] : [];
  const qs = await listQuestions(materialId, { count, tags: tags.join(","), excludeMastered: false });
  // ...设置 questions 和 questionIds
  setQuestions(qs.map(q => ({ question: q.question, options: q.options, answer: q.answer, explanation: q.explanation, tags: q.tags })));
  setQuestionIds(qs.map(q => q.id));
  setCurrentIndex(0);
  // 0 秒等待！
}, [config, materialId]);
```

- [ ] **Step 6: 删除旧的 generateQuizStream 调用相关代码**

移除 `startForge` 函数、`generateQuizStream` import、生成动画等相关代码。

- [ ] **Step 7: supplementQuestions 函数**

```tsx
const supplementQuestions = useCallback(async () => {
  await generateQuestions(materialId, true);
  // 轮询状态
  const poll = setInterval(async () => {
    const status = await getGenerateStatus(materialId);
    if (status.status === "ready") {
      clearInterval(poll);
      getMaterialStats(materialId).then(setStats);
      window.dispatchEvent(new Event("forge-activity"));
    }
  }, 2000);
}, [materialId]);
```

---

## Task 9b: 已攻克列表视图 (P1)

**Files:**
- Create: `frontend/src/components/MasteredList.tsx`
- Modify: `frontend/src/components/ForgePanel.tsx`

- [ ] **Step 1: 创建 MasteredList 组件**

```tsx
"use client";
import { type QuestionItem } from "@/lib/api";

export function MasteredList({ questions, onClose }: { questions: QuestionItem[]; onClose: () => void }) {
  if (questions.length === 0) {
    return <div className="text-text-muted text-sm py-4 text-center">暂无已攻克题目</div>;
  }
  return (
    <div className="space-y-2 max-h-64 overflow-y-auto">
      {questions.map(q => (
        <div key={q.id} className="p-3 rounded-lg bg-surface-raised border border-border-soft text-sm">
          <div className="flex items-start gap-2">
            <span className="text-green-400 shrink-0 mt-0.5">✓</span>
            <div>
              <p className="text-text-primary">{q.question}</p>
              <p className="text-text-dim text-xs mt-1">连续做对 {q.challenge_streak} 次 · 已攻克</p>
            </div>
          </div>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: ForgePanel 空闲态集成**

在错题重做和间隔复习入口下方新增可折叠区域：

```tsx
{stats.mastered_questions > 0 && (
  <details className="mt-2">
    <summary className="text-sm text-text-muted cursor-pointer hover:text-text-secondary">
      已攻克 · {stats.mastered_questions}题
    </summary>
    <MasteredList questions={allQuestions.filter(q => q.mastered)} onClose={() => {}} />
  </details>
)}
```

- [ ] **Step 3: 验证**

打开有已攻克题目的材料，展开已攻克列表，确认显示正确。

---

## Task 9c: 稳定卡片列表视图 (P1)

**Files:**
- Modify: `backend/routers/review.py` — 新增 stable-cards 端点
- Create: `frontend/src/components/StableCardList.tsx`
- Modify: `frontend/src/components/ForgePanel.tsx`

- [ ] **Step 1: 后端新增 GET /api/review/stable-cards**

```python
@router.get("/stable-cards")
def get_stable_cards(material_id: int = None, db: Session = Depends(get_db)):
    query = db.query(ReviewCard).filter(ReviewCard.is_stable == True)
    if material_id:
        query = query.join(Question).filter(Question.material_id == material_id)
    cards = query.order_by(ReviewCard.last_review.desc()).limit(50).all()
    return [{
        "review_id": c.id,
        "question_id": c.question_id,
        "question": c.question.question if c.question else "",
        "options": c.question.options if c.question else [],
        "answer": c.question.answer if c.question else "",
        "ease_factor": c.ease_factor,
        "interval_days": c.interval_days,
        "repetitions": c.repetitions,
        "total_reviews": c.total_reviews,
        "last_review": c.last_review.isoformat() if c.last_review else None,
        "is_stable": c.is_stable,
    } for c in cards]
```

- [ ] **Step 2: 手动复习重置**

在现有 `POST /api/review/record` 中：如果卡片 is_stable=true 且 score < 2，重置 is_stable=false, interval_days=1。已在上方 Task 5 的 record 端点实现中包含此逻辑（毕业判定后的分支）。

如果 score >= 2：仅更新 last_review，不改变 is_stable 和其他参数（保持稳定状态）。

```python
# 稳定卡片手动复习逻辑（在 Task 5 record 端点中补充）
if card.is_stable:
    if score < 2:
        card.is_stable = False
        card.interval_days = 1
        card.repetitions = 0
    # score >= 2: 保持稳定，仅更新 last_review
    card.last_review = datetime.utcnow()
    db.commit()
    return {"ok": True, "interval_days": card.interval_days, "is_stable": card.is_stable}
```

- [ ] **Step 3: 创建 StableCardList 组件**

```tsx
"use client";
import { useState } from "react";
import { getStableCards, recordReview, type ReviewItem } from "@/lib/api";

export function StableCardList({ materialId, onClose }: { materialId: number; onClose: () => void }) {
  const [cards, setCards] = useState<ReviewItem[]>([]);
  const [reviewingId, setReviewingId] = useState<number | null>(null);
  const [showAnswer, setShowAnswer] = useState(false);

  // load on mount
  useEffect(() => { getStableCards(materialId).then(setCards); }, [materialId]);

  if (cards.length === 0) {
    return <div className="text-text-muted text-sm py-4 text-center">暂无稳定卡片</div>;
  }

  return (
    <div className="space-y-2 max-h-80 overflow-y-auto">
      {cards.map(c => (
        <div key={c.review_id} className="p-3 rounded-lg bg-surface-raised border border-border-soft">
          <p className="text-sm text-text-primary">{c.question}</p>
          <div className="flex items-center justify-between mt-2">
            <span className="text-xs text-text-dim">
              间隔 {c.interval_days}天 · 复习 {c.total_reviews}次 · 稳定
            </span>
            <button
              onClick={() => { setReviewingId(c.review_id); setShowAnswer(false); }}
              className="text-xs px-2 py-1 rounded bg-blue-500/20 text-blue-400 hover:bg-blue-500/30 transition-colors"
            >
              手动复习
            </button>
          </div>
          {reviewingId === c.review_id && (
            <div className="mt-2 pt-2 border-t border-border-soft">
              {!showAnswer ? (
                <button onClick={() => setShowAnswer(true)} className="text-xs text-brand hover:underline">显示答案</button>
              ) : (
                <div>
                  <p className="text-xs text-green-400">答案: {c.answer}</p>
                  <div className="flex gap-1 mt-2">
                    {[0, 1, 2, 3].map(s => (
                      <button key={s} onClick={async () => {
                        await recordReview(c.question_id, s);
                        setCards(prev => prev.filter(x => x.review_id !== c.review_id));
                        setReviewingId(null);
                      }} className="text-xs px-2 py-1 rounded bg-surface-raised hover:bg-surface-hover border border-border-soft">
                        {s}
                      </button>
                    ))}
                  </div>
                </div>
              )}
            </div>
          )}
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: ForgePanel 空闲态集成**

在已攻克列表下方新增可折叠区域：

```tsx
{stats.stable_cards > 0 && (
  <details className="mt-2">
    <summary className="text-sm text-text-muted cursor-pointer hover:text-text-secondary">
      稳定卡片 · {stats.stable_cards}张
    </summary>
    <StableCardList materialId={material.id} onClose={() => {}} />
  </details>
)}
```

- [ ] **Step 5: 验证**

打开有稳定卡片的材料，展开稳定卡片列表，点击"手动复习"评分确认重置逻辑。

---

## Task 10: KnowledgePanel — 材料卡片出题状态 (P1)

**Files:**
- Modify: `frontend/src/components/KnowledgePanel.tsx`

- [ ] **Step 1: 读取 question_count 显示状态**

```tsx
{s ? (
  <span>
    {s.total_questions > 0
      ? `正确率 ${s.accuracy}% · ${s.total_questions}题`
      : "出题中..."}
    {s.due_reviews > 0 && <span className="text-yellow-400 ml-1">待复习{s.due_reviews}</span>}
  </span>
) : (
  "加载中..."
)}
```

- [ ] **Step 2: 点击材料时如果题目数为 0，自动触发 generate**

```tsx
useEffect(() => {
  if (stats && stats.total_questions === 0) {
    generateQuestions(materialId).catch(() => {});
    // 开始轮询
    const poll = setInterval(async () => {
      const status = await getGenerateStatus(materialId);
      if (status.status === "ready" || status.status === "error") {
        clearInterval(poll);
        loadData();
      }
    }, 2000);
    return () => clearInterval(poll);
  }
}, [materialId, stats?.total_questions]);
```

---

## Task 11: UploadModal — 上传后触发 generate (P1)

**Files:**
- Modify: `frontend/src/components/UploadModal.tsx:34-56`

- [ ] **Step 1: 上传成功后触发异步出题**

```tsx
const result = await uploadMaterial(...);
// 上传成功后自动触发出题
generateQuestions(result.id).catch(() => {});
// ... 显示 chunk 反馈 + 关闭
```

---

## Task 12: page.tsx — 全局复习拆两个入口 (P2)

**Files:**
- Modify: `frontend/src/app/page.tsx:67-72`

- [ ] **Step 1: 两个全局按钮替换原来的"今日复习"**

```tsx
{globalStats && globalStats.wrong_questions > 0 && (
  <button onClick={() => setGlobalReview("wrong")} className="...">
    错题重做（{globalStats.wrong_questions}题）
  </button>
)}
{globalStats && globalStats.due_reviews > 0 && (
  <button onClick={() => setGlobalReview("sm2")} className="...">
    间隔复习（{globalStats.due_reviews}题）
  </button>
)}
```

- [ ] **Step 2: GlobalReview 组件接收 mode prop**

---

## Task 13: 验收脚本 (P2)

**Files:**
- Create: `tests/v4.0-acceptance.py`

- [ ] **Step 1: 编写验收脚本**

覆盖 F1-F9 + 回归的 63 条验收项核心场景。

- [ ] **Step 2: 运行**

```bash
python3 tests/v4.0-acceptance.py
```

Expected: 核心验收项全部通过。

---

## 执行顺序

```
Task 1 (Question schema)
  ↓
Task 2 (ReviewCard schema)
  ↓
Task 3 (异步出题端点) ← 最复杂
  ↓
Task 4 (submit + streak) ← 依赖 Task 1
  ↓
Task 5 (复习毕业 + wrong-questions) ← 依赖 Task 2
  ↓
Task 6 (统计更新) ← 依赖 Task 1, 2
  ↓
Task 7 (前端 API) ← 依赖 Task 3-6
  ↓
Task 8 (ReviewPanel 组件) ← 依赖 Task 7
  ↓
Task 9 (ForgePanel 改造) ← 依赖 Task 7, 8
  ↓
Task 9b (已攻克列表) + Task 9c (稳定卡片) ← 依赖 Task 7, 9
  ↓
Task 10 (KnowledgePanel) ← 依赖 Task 7
  ↓
Task 11 (UploadModal) ← 依赖 Task 7
  ↓
Task 12 (全局复习) ← 依赖 Task 8
  ↓
Task 13 (验收脚本) ← 全部完成
```

前 6 个 Task 是纯后端，可连续执行。Task 7-12 是纯前端，按依赖顺序。Task 13 最后。
